import Typography from "@mui/material/Typography";

export const Card = () => {
    return (
        <Typography
        component="h1"
        variant="h6"
        color="inherit"
        noWrap
        sx={{ flexGrow: 1 }}
    >
        OUTLET TEXT
    </Typography>
    );
}